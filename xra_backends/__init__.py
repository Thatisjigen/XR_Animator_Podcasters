"""XR Animator native MediaPipe Tasks backend (server-side).

The bundled application contains Python, MediaPipe, OpenCV and both Tasks
models; the target machine does not need Python or extra packages installed.

Modules
-------
registry  : catalogue of bundled model definitions + on-disk layout.
"""
